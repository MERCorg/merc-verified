name: Lean Action CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
      with:
        submodules: true
        
    - name: Install and build Lean
      uses: leanprover/lean-action@v1

    - name : Build Lean code
      run: lake build Signatures
